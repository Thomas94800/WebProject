extends layout
block content

  .sectionQuizResult
  
    h2 Quiz Result - Divers information about the World

    each item, index in feedback
      div
        p= `${questions[index].question}` 
        p= `Your Answer - ${item.userAnswer}, Correct Answer - ${item.correctAnswer}, ${item.isCorrect ? 'Correct' : 'Incorrect'}`

    p= `Final Grade: ${percentageGrade.toFixed(2)}%`

    button 
      a(href="/quizzes") Return to Quizzes